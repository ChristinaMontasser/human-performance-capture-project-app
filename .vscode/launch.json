{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Tkinter",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/frontend/main.py",
            "console": "integratedTerminal",
            "env": {
                "WORKSPACE_FOLDER": "${workspaceFolder}"
            }
        }
    ]
}